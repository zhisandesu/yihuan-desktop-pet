异环小精灵 v1.0.2 · Windows x64 免安装版

1. 先完整解压文件夹，再双击 YihuanCompanion.exe。不要在压缩包里直接运行。
2. 已包含 .NET 桌面运行环境，无需安装开发工具。适用于 Windows 10/11 64位（x64）。
3. 右击角色打开菜单。拖动角色移动位置；在设置中调整大小、动作与陪伴模式。
4. 本包已预置安魂曲中／日复刻音色 ID 和语音参数，不含作者的 API Key 或聊天记录。
5. 聊天：设置 → API，选择自己的服务，填写自己的 Key 与可用模型，再保存。
6. 语音：默认中文并开启回复朗读。使用阿里 Token Plan 聊天时，只需填写一次 Key，语音会共用这份 Key；无需重复填写模型、音色和采样率。使用其他聊天服务时，在“更换语音 Key”里填写阿里 Token Plan Key。
7. 日配只改变朗读语言；日常聊天、气泡与互动记录默认显示中文。
8. 未配置 API 也能使用角色展示、拖拽、菜单与本地动作；联网聊天、语音和主动台词需要对应服务。

音色权限与排查
预置音色 ID 不会转移云端账号权限。当前 Key 必须能够调用这些复刻音色；若服务提示音色不存在或无权限，在“高级 · 更换复刻音色”中填写自己账号的复刻音色 ID。未验证任意其他账号均可使用预置 ID，不会回退到系统默认声音。
先点“试听当前复刻音色”。若试听正常而聊天未朗读，检查“回复后实时朗读”和“安静陪伴”开关；聊天状态栏会显示关闭或合成错误原因。

隐私与费用
Key 与聊天记录保存在当前 Windows 用户的本机数据目录，使用系统加密；密钥不写入本程序资源。
聊天会把输入、必要的上下文和已保存记忆发送到你选择的服务；语音识别会上传录音，朗读会上传待朗读文字。模型与语音费用由你与服务商结算。
本机数据目录：%LOCALAPPDATA%\YihuanCompanion。删除数据前先自行备份。

先阅读“免责与使用声明.txt”。第三方组件的版权与许可见“第三方许可”目录。
本包未做代码签名；本机验证通过不代表已经覆盖所有电脑和驱动环境。
本次修复：互动时以“我／我的”自然回应触碰，不用“她／它”称呼自己；请求状态改为“正在等待回复……”，不再与角色台词混淆。
